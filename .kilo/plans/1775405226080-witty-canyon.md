# Plan: Create AGENTS.md

## Task

Create an `AGENTS.md` file at `D:\Projects\alexzedim\cmnw-next\AGENTS.md` (~150 lines) containing build/lint/test commands and code style guidelines for agentic coding agents.

## Analysis Summary

### Build/Lint/Test Commands (from package.json)

- **Dev:** `pnpm dev` (uses Turbopack)
- **Build:** `pnpm build`
- **Lint:** `pnpm lint` (runs `eslint --fix`)
- **Format:** `pnpm format` (Prettier)
- **No test framework** is configured (no test scripts, no test dependencies)

### Key Conventions Discovered

- **Package manager:** pnpm
- **Framework:** Next.js 16 (App Router) + React 19 + TypeScript 5.6
- **Styling:** Tailwind CSS 3 + CSS custom properties + Tailwind Variants
- **UI Library:** HeroUI + Tremor charts
- **Data fetching:** SWR (client) + direct fetch (server components/API routes)
- **Components:** Named exports with arrow functions, `kebab-case` filenames
- **Types:** `interface` for props/entities, `type` for aliases/unions, `enum` for domain values
- **Imports:** `@/*` alias, strict ordering, `import type` separated
- **ESLint:** import ordering, unused imports removal, JSX prop sorting, padding before return
- **Prettier:** double quotes, 2-space indent, 80 char width, trailing comma es5, semicolons
- **No Cursor/Copilot rules** found

## Implementation

1. Write `AGENTS.md` with the following sections:
   - Project overview (brief)
   - Build & Development Commands
   - Code Style & Conventions (imports, formatting, types, naming, components, styling, error handling, server/client split)
